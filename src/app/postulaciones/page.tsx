import { AppShell } from "@/src/components/app-shell"
import { PostulacionesList } from "@/src/components/postulaciones-list"

export default function PostulacionesPage() {
  return (
    <AppShell>
      <PostulacionesList />
    </AppShell>
  )
}
